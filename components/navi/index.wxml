<!--components/navi/index.wxml-->
<view class="container">
<image bind:tap="onLeft" class="icon-arrow" src="{{latest ? leftImgGray : leftImg}}"></image>
<text class="title">{{title}}</text>
<image bind:tap="onRight" class="icon-arrow" src="{{first ? rightImgGray : rightImg}}"></image>
</view>